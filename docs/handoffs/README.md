# Handoffs Directory

Each review checkpoint produces a handoff doc here named CP-<N>-<tag>.md.

Template: see `docs/review-protocol.md` § Handoff Doc Template.

Handoffs are committed alongside the tag, so the review trail is reproducible.

